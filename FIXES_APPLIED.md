# التعديلات اللي اتعملت على المشروع

1. **إصلاح خطأ بناء (build-breaking)**: أضفنا استيراد `OrderActions` الناقص في
   `src/app/admin/orders/page.tsx` — كان بيسبب خطأ TypeScript ويوقف الـ build.

2. **إصلاح مسار صفحة المخازن**: نقلنا `page.tsx` و`warehouse-form.tsx` من المسار
   المتداخل الغلط `src/app/admin/warehouses-feature/src/app/admin/warehouses/`
   إلى المكان الصحيح `src/app/admin/warehouses/`، ومسحنا الفولدر الزيادة بالكامل
   (بما فيه فولدر فاضي زيادة كان اسمه حرفيًا
   `{customers,products,orders,pricing,invoices,leads,reports}`).

3. **إضافة رابط "المخازن" و`force-dynamic`** في `src/app/admin/layout.tsx` —
   بدونها الصفحة كانت هتتحاول تتبني static وقت الـ build وتفشل.

4. **استرجاع نظام الموافقة على المنتجات (Product Approval)** اللي كان ناقص بالكامل:
   - حقول جديدة في `src/db/schema.ts`: `spaceM2`, `requestedInitialQty`,
     `approvalStatus`, `rejectionReason` على جدول `products`.
   - migration جديد: `drizzle/0001_long_hammerhead.sql`.
   - route جديد: `src/app/api/products/[id]/approve/route.ts`.
   - كومبوننت جديد: `src/app/admin/products/approval-actions.tsx`.
   - تحديث `src/app/admin/products/page.tsx` ليعرض قسم "منتجات بانتظار الموافقة".
   - تحديث `src/app/api/products/route.ts`: منتجات العملاء (role = CUSTOMER)
     بقت تتسجل بحالة `pending` ومتضافش للمخزون إلا بعد موافقة الأدمن.

5. **إصلاحات تنسيقية بسيطة**: استبدال علامات تنصيص عربي مباشرة بـ `&quot;` في
   3 ملفات (`page.tsx`, `pricing/calculator.tsx`, `customer/orders/orders-view.tsx`)
   عشان تتوافق مع قواعد الـ linter.

## قبل ما تنشر التحديث ده

المشروع محتاج migration جديد على قاعدة البيانات الفعلية (D1) عشان الحقول
الجديدة تتضاف فعليًا. شغّل الأمر ده بعد فك الملف وتثبيت الحزم:

```
npm install
npm run db:migrate:remote
```

بعدها انشر عادي بـ:
```
npm run cf:deploy
```

## أزرار الحذف المضافة حديثًا

- **المنتجات** (`admin/products`): زرار "حذف" في آخر كل صف — ظاهر لـ Super Admin فقط.
  الـ API كان موجود بالفعل (`DELETE /api/products/[id]`).
- **المخازن** (`admin/warehouses`): زرار "حذف" في آخر كل صف — لأي Admin أو Super Admin.
  الـ API لم يكن موجودًا فأنشأناه (`DELETE /api/warehouses/[id]`)، وهو يرفض حذف
  أي مخزن مرتبط بمساحة تخزين نشطة لعميل، لتفادي فقدان بيانات العملاء بالخطأ.
- **العملاء** (صفحة تفاصيل العميل): زرار "حذف العميل نهائيًا" جنب زرار تعليق
  الحساب — ظاهر لـ Super Admin فقط، ومطلوب تأكيد قبل التنفيذ.
  الـ API كان موجودًا بالفعل (`DELETE /api/customers/[id]`).

كل أزرار الحذف بتطلب تأكيد من المستخدم (نافذة confirm) قبل التنفيذ الفعلي.

## تحديث: حذف العميل بقى شامل (Cascade Delete) + سكريبت تصفير البيانات

- **زرار حذف العميل بقى بيشتغل حتى لو العميل مربوط بمساحة تخزين نشطة** أو
  عنده أي بيانات مرتبطة (طلبات، فواتير، منتجات، استلام، مرتجعات...). أضفنا
  دالة `deleteCustomerCascade` في `src/lib/customer-cleanup.ts` بتمسح كل
  البيانات المرتبطة بالعميل بالترتيب الصح (من الأبناء للآباء) قبل ما تمسح
  العميل نفسه، فمفيش قيود foreign key هتوقف الحذف تاني.

- **سكريبت تصفير فوري لكل العملاء والمنتجات**: `scripts/reset-customers-products.sql`
  — يمسح كل العملاء والمنتجات وكل حاجة مرتبطة بيهم (طلبات، فواتير، مخزون،
  استلام، مرتجعات...)، ويسيب المخازن وإعدادات التسعير وحسابات الموظفين
  (Admin/Warehouse Employee) والـ leads زي ما هي. شغّله مرة واحدة بس على
  قاعدة بياناتك الحية بالأمر ده من جوه فولدر المشروع (بعد `npm install`):

  ```
  npx wrangler d1 execute storage-platform-db --remote --file=./scripts/reset-customers-products.sql
  ```

  ملحوظة: الأمر ده **بيمسح البيانات فعليًا ونهائيًا** من قاعدة البيانات
  الحية، ومفيش تراجع بعده. لو عايز تجرب الأمر الأول من غير خطورة، شيل
  `--remote` من الأمر عشان يشتغل على نسخة التطوير المحلية بس.

## تحديث: اختيار المخزن للعميل — تحسين عرض المساحة المتاحة

الميزة الأساسية (العميل يشوف كل المخازن اللي أضافها السوبر أدمن ويختار
يخزن في انهي واحد) **كانت موجودة بالفعل**. اللي أضفناه هو تحسين مهم:

- صفحة `customer/storage` بقت تحسب **المساحة المتاحة الفعلية** لكل مخزن
  (السعة الكلية ناقص كل اللي محجوز فيه من أي عميل تاني)، مش بس السعة الكلية.
- القائمة المنسدلة في `storage-booking.tsx` بقت تعرض "متاح X م²" لكل مخزن،
  وتمنع اختيار أي مخزن ممتلئ بالكامل (بيظهر "(ممتلئ)" ومعطّل).
- حقل "المساحة" بقى بيوضح تحته أقصى مساحة متاحة في المخزن المختار، ومقفول
  عليها كحد أقصى (attribute `max`) عشان يقلل محاولات الحجز الفاشلة.
- لو كل المخازن ممتلئة، بتظهر رسالة واضحة للعميل بدل نموذج حجز مضلِّل.
